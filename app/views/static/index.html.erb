<h1>Welcome: <%= @username %></h1>
<!--<h4>You have: %= pluralize(current_user.lists.count, 'List', 'Lists') %></h4> -->

<%= link_to 'All Bucketlists', lists_path %>

<%= link_to 'Profile', edit_user_registration_path %>

<%= link_to 'Logout', destroy_user_session_path, method: :delete, data: {confirm: 'Really want to leave Bucketlist?'} %>
