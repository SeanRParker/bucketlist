<h1>Edit <%= @item.name %></h1>

<%= render(partial: 'form') %>

<br />
<%= link_to 'Delete Item', list_item_path(@list, @item), method: :delete, data: {confirm: 'Sure you want to delete this?'}, class: "waves-effect waves-light btn red lighten-1" %>
